APP_NAME="FORSA"
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080

DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=forsa
DB_USERNAME=fadil
DB_PASSWORD=
DB_SCHEMA=forsa

SESSION_NAME=forsa_session
UPLOAD_MAX_SIZE_MB=15

# FORSA AI Assistant (docs/ai-assistant/PRD.md) — used starting Phase 2, not yet Phase 1.
MODEL_API_KEY=
MODEL_NAME=gpt-4o-mini

# Dedicated read-only Postgres role for the AI Analytics Service (Phase 7).
# Create it first: see database/migrations/009_create_ai_reader_role.sql.
AI_DB_USERNAME=forsa_ai_reader
AI_DB_PASSWORD=

# Mascot greeting rotation interval, in milliseconds (PRD §27 default: 10000).
MASCOT_GREETING_INTERVAL_MS=10000

# Full mascot walk from one corner to the other, in milliseconds.
MASCOT_WALK_DURATION_MS=6000
